runConfig:
  maxInstances: 1
  memoryMiB: 1024

scripts:
  buildCommand: npm install && npm run build

env:
  - variable: MAKE_WEBHOOK_URL
    value: https://hook.eu1.make.com/detvatwu4ci12qn9y1dxbih42a1redyt
    availability:
      - RUNTIME
  # --- รีวิว Google บนหน้าแรก (src/app/api/google-reviews) ---
  # 1) Google Cloud Console → เปิด "Places API (New)" → สร้าง API key (จำกัด API เฉพาะ Places API (New))
  # 2) เก็บ key เป็น secret:  firebase apphosting:secrets:set GOOGLE_PLACES_API_KEY
  # 3) เอา comment ออก 4 บรรทัดด้านล่างแล้ว deploy — ถ้าไม่ตั้งค่า ส่วนรีวิวสดจะไม่แสดง แต่เว็บทำงานปกติ
  # - variable: GOOGLE_PLACES_API_KEY
  #   secret: GOOGLE_PLACES_API_KEY
  #   availability:
  #     - RUNTIME
  # (ไม่บังคับ) Place ID แบบ ChIJ… — ดูได้จาก log "[google-reviews] พบ Place ID" หลัง deploy ครั้งแรก
  # - variable: GOOGLE_PLACE_ID
  #   value: ChIJ...
  #   availability:
  #     - RUNTIME
