import { permanentRedirect } from "next/navigation";

/**
 * URL นี้ถูกยุบไปรวมกับ /visa-work-permit แล้ว
 *
 * สองหน้านี้เคยเป็นบริการเดียวกันแต่คนละภาษา Google จึงต้องเลือกหนึ่ง
 * และสัญญาณถูกแบ่งครึ่ง ตอนนี้เหลือหน้าเดียวที่ URL ตรงกับคำค้นหาจริง
 *
 * ตัวส่งต่อหลักอยู่ใน next.config.ts (ทำงานก่อนถึง React)
 * ไฟล์นี้เป็นชั้นสำรอง เผื่อ config ถูกแก้หรือหลุดไป และเพื่อให้คนที่
 * มาอ่านโค้ดเห็นว่าหน้านี้ย้ายไปไหน
 *
 * ห้ามลบทิ้งเฉยๆ — ลิงก์เก่าที่คนอื่นแชร์ไว้ และอันดับที่ Google เก็บไว้
 * ต้องมีทางเดินต่อ ไม่งั้นกลายเป็น 404 แล้วเสียของที่สะสมมาทั้งหมด
 */
export default function ExpatServicesRedirect() {
  permanentRedirect("/visa-work-permit");
}
